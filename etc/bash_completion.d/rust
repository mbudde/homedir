if [[ -d ~/.multirust ]]; then
    default=$(cat ~/.multirust/default)
    dir=~/.multirust/toolchains/$default/etc/bash_completion.d
    for f in $(ls $dir); do
        f=$dir/$f
        [[ -f $f && -r $f ]] && . "$f"
    done
fi
