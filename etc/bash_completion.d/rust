if [[ -d ~/.rustup ]]; then
    sysroot=$(rustc --print sysroot)
    dir=$sysroot/etc/bash_completions.d
    for f in $(ls $dir); do
        f=$dir/$f
        [[ -f $f && -r $f ]] && . "$f"
    done
fi
